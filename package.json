{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "\"My portfolio\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node run.js",
    "dev": "concurrently \"npm start dev\" \"live-server dist\"",
    "build": "node run.js build",
    "deploy": "cd utils && node gh-pages.js",
    "sass": "node-sass --watch src/scss -o dist/css --output-style compressed"
  },
  "author": "Nischay Sharma",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.5.2",
    "concurrently": "^6.4.0",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "gh-pages": "^3.2.3",
    "node-sass": "^6.0.1",
    "shelljs": "^0.8.4"
  }
}
